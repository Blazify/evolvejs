{
    "breaking_changes": [
        "`- <ShardManager>.shutdown()`\n `+ <ShardManager>.destroyAll(code)",
        "`+ <ShardManager>.reconnect(shardId)`\n `+ <ShardManager>.destroy(shardId)`"
    ],
    "changes": [
        "Fixed API RateLimiting",
        "Fixed newMessage not being emitted",
        "Fixed Decorator Errors",
        "Some large unnecessary properties are now non-enumerable and non-writable",
        "No 404s are emmitted but if exceptions occur, we handle.",
        "Deno Releases are Instant"
    ]
}